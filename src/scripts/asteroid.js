const asteroid = (prom)=>{
  prom.then(data =>{
      // let earthImage = document.querySelector('.earth__image')
      // earthImage.style.backgroundImage=`url(${data.url})`
      console.log(data)
  })

}
export default asteroid;